# Карусель-слайдер

Інтерактивний компонент карусель-слайдер з розширеними можливостями для веб-сайтів.

## Особливості

- 🎯 Автоматичне перегортання слайдів
- 🎮 Зручне керування (кнопки пауза/плей, вперед/назад)
- 📱 Підтримка свайпів на мобільних пристроях
- ⌨️ Керування з клавіатури (стрілки вліво/вправо, пробіл)
- 🎨 Індикатори поточного слайду
- ⚙️ Гнучкі налаштування (інтервал, автозапуск, пауза при наведенні)

## Встановлення

1. Клонуйте репозиторій
2. Встановіть залежності:

```bash
npm install
# або
yarn
```

> Для використання yarn він має бути встановлений глобально:
>
> ```bash
> npm i -g yarn
> ```

## Налаштування

Карусель можна налаштувати при ініціалізації:

```javascript
const carousel = new SwipeCarousel({
  containerId: '#carousel', // ID контейнера карусели
  slideId: '.item', // Клас слайдів
  interval: 5000, // Інтервал автоматичного перегортання (мс)
  isPlaying: true, // Автозапуск
  pauseOnHover: false // Пауза при наведенні
})
```

## Запуск тестів

```bash
npm test
# або
yarn test
```

## Розробка

Проект має модульну структуру:

- `core.js` - базовий функціонал карусели
- `swipe.js` - розширення для підтримки свайпів
- `config.js` - налаштування за замовчуванням та константи
